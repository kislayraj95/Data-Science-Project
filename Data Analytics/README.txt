ENG2006_Coursework_2_Final.ipynb file contains the demanded code with desired output.

All the test cases are passed with great graph and visuals.

The path for the directories for question 2 need to be changed in the code. The path needs to be set up according to user local file storage.

A Combined_Front directory needs to be created inside test and train folder which contains all the images present in ok_front and def_front.
example: Combined_Front -> contains all image from ok_front and def_front inside test directory.
         Combined_Front -> contains all image from ok_front and def_front inside train directory.

The final model for question 2 is saved in imageModel as mentioned in the question.

This folder contains all the necessary files which is needed by the project.